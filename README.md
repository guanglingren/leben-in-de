# 德国浮生记

一款以德国普通居民日常生活为背景的手机网页生存模拟游戏。

## 本地运行

```bash
npm install
npm run dev
```

打开 `http://localhost:3000`。

## Render 部署

- Build Command: `npm install && npm run build`
- Start Command: `npm run start`
- Node 版本：20 或更新

Cloudflare 中将域名的 CNAME 指向 Render 提供的地址，并按 Render 提示完成自定义域名验证。
